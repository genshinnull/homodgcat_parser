[project]
name = "homodgcat_parser"
version = "0.0.0"
description = ""
requires-python = ">=3.14"
dependencies = ["gitpython", "marimo", "orjson", "polars", "pydantic", "pysubs2"]

[dependency-groups]
dev = ["marimo[recommended,lsp,mcp]", "ty"]
